/* Carousel */
(function() {
	var aboutTyped = new Typed('#iam-carousel', {
		strings: ["hi there, i'm libby :-)"],
		typeSpeed: 120,
		startDelay: 200,
		fadeOut: false,
		loop: false,
		showCursor: false,
	});
});

/* AOS */
AOS.init({ duration: 1000, once: true });
